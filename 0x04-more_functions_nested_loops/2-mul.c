#include "main.h"
/**
 * mul - Entry point
 * Description: print all lowercase alphabit except e and q
 * @a: it is an input
 * @a: it is an input
 * Return: return(0)
 */
int mul(int a, int b)
{
	int num;

	num = a * b;
	return (0);
}
