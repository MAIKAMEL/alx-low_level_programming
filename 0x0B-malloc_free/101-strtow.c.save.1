#include "main.h"

/**
 * wordcount - count the number of word
 * @s: string to count
 * Return: int of number of word
 */

int wordcount(char *s)
{
	int i, n = 0;

	for (i = 0; s[i]; i++)
	{
		if (s[i] == ' ')
		{
			if (s[i + 1] != ' ' && s[i + 1] != '\0')
				n++;
		}
		else if (i == 0)
			n++;
	}
	n++;
	return (n);
}


/**
 * **strtow - splits string into words
 * @str: string to count
 * Return: int of number of word
 */
char **strtow(char *str)
{
	int i, j, k, l, n = 0, wc = 0;
	char **w;

	if (str == NULL || str == '\0')
		return (NULL);
	n = wordcount(str);
	if (n == 1)
		return (NULL);
	w = (char **)malloc(n * sizeof(char *));
	if (w == NULL)
		return (NULL);
	w[n - 1] = NULL;
	i = 0;
	while (str[i])
	{
		if (str[i] != ' ' && (i == 0 || str[i - 1] == ' '))
		{
			for (j = 1; str[ i + j] != ' ' && str[i + j]; j++)
			;
			j++;
			w[wc] = s
