#include "main.h"
/**
 * _abs - Entry point
 * Description: print all lowercase alphabit except e and q
 * @int: it is an input
 * Return: return(0)
 */
int _abs(int)
{
	if (int < 0)
	{
		int = (-1) * int;
	}
	return (int);
}
